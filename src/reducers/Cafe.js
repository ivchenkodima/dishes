import { RESTAURANT } from '../data';

export default function SortCafe(state = RESTAURANT, action) {
    switch (action.type) {
        // case 'FIND_CAFE_SEARCH':
        //     return [...state,
        //     ];
        default:
            return state;
    }

}